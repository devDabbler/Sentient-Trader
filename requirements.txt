streamlit>=1.39.0
requests>=2.31.0
pandas>=2.0.0
yfinance>=0.2.18
numpy>=1.24.0
python-dotenv>=1.0.0
plotly>=5.14.0
openai>=1.0.0
anthropic>=0.8.0
google-generativeai>=0.3.0
pytest
pytest-asyncio
scipy
matplotlib
ib_insync>=0.9.86
discord.py>=2.3.0
pyqlib
supabase>=2.0.0
crawl4ai>=0.7.0
playwright>=1.40.0  # Browser automation for Crawl4AI (run: playwright install chromium)
nest-asyncio>=1.5.0  # Critical for Windows async subprocess support
praw>=7.7.0  # Reddit API for social sentiment (avoids rate limiting)
feedparser>=6.0.0  # Fast RSS feed parsing for Reddit (no rate limits!)
httpx>=0.24.0  # Modern async HTTP client for RSS feeds
apscheduler>=3.10.0  # Scheduled event detection
selenium>=4.38.0  # Web browser automation for human-like behavior
psutil>=5.9.0  # System and process utilities for resource monitoring
gs-quant

transformers>=4.30.0  # Hugging Face transformers for FinBERT model
torch>=2.0.0  # PyTorch for FinBERT (CPU version sufficient, GPU optional)
# Note: For GPU support, install: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# Performance Optimization Libraries
ta-lib>=0.4.25  # Ultra-fast technical indicators (C implementation)
numba>=0.58.0  # JIT compilation for mathematical operations
polars>=0.20.0  # Fast DataFrame operations (5-10x faster than pandas)
redis>=4.5.0  # High-speed caching (evaluate vs Supabase)
tenacity>=8.2.0  # Robust retry logic with exponential backoff
circuitbreaker>=1.4.0  # Circuit breaker pattern for resilience
loguru>=0.7.0  # Fast, efficient logging with minimal boilerplate
pywin32>=306; sys_platform == 'win32'  # Windows services support (Phase 3)

# Solana DEX Hunter - Phase 1: On-chain Verification
base58>=2.1.1  # Base58 encoding for Solana addresses

# Service Control Panel - 2FA Authentication
pyotp>=2.9.0  # TOTP one-time passwords (Google Authenticator)
qrcode[pil]>=7.4.0  # QR code generation for 2FA setup

# Signal Memory RAG - Vector Embeddings for Trading Pattern Memory
# Uses Ollama (local, FREE) by default. Set SIGNAL_MEMORY_EMBEDDING_PROVIDER=openai for cloud.
# For Ollama local embeddings: ollama pull nomic-embed-text
sentence-transformers>=2.2.0  # Optional: Local embeddings (alternative to Ollama)
